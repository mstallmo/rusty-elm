{
  "author": "Mason Stallmo <masonstallmo@gmail.com>",
  "name": "rusty-elm",
  "version": "0.0.1",
  "license": "(MIT OR Apache-2.0)",
  "scripts": {
    "start:dev": "webpack-dev-server -d && bsb -make-world -w",
    "start": "npm run build:client & npm run build:server",
    "build:client": "bsb -make-world -w && webpack -w",
    "build:server": "cargo run --manifest-path server/Cargo.toml",
    "build": "webpack && cargo build --manifest-path server/Cargo.toml",
    "build:elm": "elm make elm/Main.elm --output=.build/main.js",
    "build:reason": "bsb -make-world",
    "webpack": "webpack"
  },
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "0.2.7",
    "bs-platform": "^5.0.3",
    "elm-webpack-loader": "^5.0.0",
    "html-webpack-plugin": "^3.2.0",
    "webpack": "^4.30.0",
    "webpack-cli": "^3.3.0",
    "webpack-dev-server": "^3.3.1"
  }
}
